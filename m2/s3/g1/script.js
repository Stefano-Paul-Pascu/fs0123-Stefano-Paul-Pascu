let User = function (firstName, lastName, age, location) {
    this.firstName = firstName;
    this.lastName = lastName;
    this.age = age;
    this.location = location;
}

let Marco = new User ('Marco', 'Rossi', 32, 'Roma');
console.log(Marco);

let Maria = new User ('Maria', 'Verdi', 28, 'Milano');
console.log(Maria);

function comparazione(){
    
}